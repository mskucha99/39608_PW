m /[]plkomkncusing System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    public int x = 5;
    // Start is called before the first frame update
    void Start()
    {
        print("Test "+x);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
